#ifndef __TPC_UTIL_ARITH_HPP
#define __TPC_UTIL_ARITH_HPP

#include <stdint.h>

namespace Util {
  namespace Arith {
    uint8_t min(const uint8_t a, const uint8_t b);
    uint16_t min(const uint16_t a, const uint16_t b);
  }
}

#endif
